# 共通部分構造の一致条件と比較・集計方法 v0.1

意味と条件を保持した部分構造を切り出し、同じ構造を持つ断片をグループ化して、支持する文書を数える。文書全体の全ペアをLLMで比較する必要はない。

本書は、仮想ノウハウ20件の実験に基づく現行方式と追加提案を整理したものである。構造化仕様は`knowhow-graph-0.1`、現行の比較方式は`strict`を前提とする。

## 1. 共通部分とする一致条件

| 項目 | 一致条件 |
|---|---|
| ノード型 | `ACTION / CHANGE / QUANTITY`などが一致 |
| 行動・状態・量の意味 | 正規化した概念IDが一致。「所要時間」と「利用可能時間」は区別 |
| 増減 | `INCREASE / DECREASE`などが一致 |
| 関係の意味 | `CAUSES / SIGNALS / REQUIRES`などが一致 |
| 接続 | role・方向・つながり方が一致 |
| 対象の参照 | 同じ対象を指す関係が対応先でも保持される |
| 否定・不確実性・文脈 | 一致必須 |
| 条件 | 有無・意味・比較演算子・値・単位が一致 |
| 論理結合 | `AND / OR / NOT`を区別し、全要素を保持 |
| 対象の具体名・種類 | 具体名は置換可能。種類は一致必須にせず、対応情報として保存 |
| ID・原文の表現 | 比較キーに使わず、根拠追跡用に保存 |

ノードの対応は一対一とする。未登録概念や未解決情報は、何にでも一致する値として扱わず、影響する断片を保留する。`null`同士でも、意味が同じとは判断しない。

例えば、次の二つでは、対象と活動の具体名を置き換えて対応づける。

```text
人員を追加 → 説明時間が増加
製品種類を追加 → 段取り時間が増加
```

```text
対象Xを追加 → 活動Yの所要時間が増加
```

上の図は簡略表示である。実際の比較では、原文の不確実性・文脈等も保持する。「所要時間」を「利用可能時間」に置き換えたり、「原因」を「兆候」に読み替えたりすることは許さない。

対象名を置き換えても、同じ対象への参照は保持する。例えば「装置Xの始動には装置Xの温度条件が必要」と「装置Xの始動には装置Yの温度条件が必要」は区別する。

この一致が示すのは、設定した抽象化の下で表現が一致することである。対象の置換や原理としての妥当性は別途確認する。

## 2. 比較する部分構造の切り出し

現行実装では、意味関係を表す`CLAIM`が1個、または2個の連結断片を探索する。

```text
構造化された各文書
  ↓
CLAIMを選択
  ↓
引数・量・対象・条件・論理要素を含める
  ↓
比較用の断片
```

例えば、`A → B → C`と`A → B → D`から、共通する`A → B`を取り出せる。ただし、選んだ関係に必要な情報は削らない。

- 条件付きの因果から条件だけを外さない。
- `A AND B → C`から`A → C`を作らない。
- 量の意味や、その量が属する対象を省略しない。
- 未解決の限定が関係する断片は保留する。

文書全体が同じ構造である必要はない。ある部分に未解決情報があっても、それに依存しない別の断片は探索できる。

## 3. 構造の比較とグループ化

比較は、軽い絞り込みと精密な判定に分ける。

| 段階 | 処理 |
|---|---|
| 入力検査 | ID・参照・根拠・辞書・接続先の型などを検査 |
| 比較ラベル作成 | IDや具体名を外し、一致必須の意味情報を残す |
| 粗い絞り込み | ノード数・接続数・ラベル別件数・role別件数が同じ断片を集める |
| 精密比較 | 属性と方向を保持したグラフ同型判定を行う |
| 登録 | 一致する代表パターンへ追加。なければ新しいパターンを作る |

グラフ同型判定では、ノードを一対一に対応づけたとき、比較ラベルとすべての接続が一致するかを確認する。粗いキーが同じだけでは一致としない。

```text
各文書 → 断片を列挙
             ↓
       粗いキーで振り分け
             ↓
       同じキーの代表パターンと精密比較
             ↓
       パターンごとに出現事例を集約
```

既存グループの代表と比較するため、文書全体の全ペア比較は不要である。ただし、同型判定自体の計算負荷は残る。現行実装は枝刈り付きの完全バックトラックで、小さな断片に制限している。任意サイズの最大共通部分グラフを求める実装ではない。

## 4. パターンごとの保存・集計

| 保存・集計する項目 | 用途 |
|---|---|
| 代表グラフと比較ラベル | 何が一致したかを示す |
| 各事例へのノード・接続対応表 | 一致箇所を再確認する |
| 原文根拠 | 構造化と解釈を確認する |
| 対象の置換情報 | 異分野間の対応の妥当性を確認する |
| 支持文書数 | 異なる何文書に現れたか |
| 出現回数 | 同一文書内の反復も含め、何回現れたか |
| 関係数 | パターンの大きさを示す。意味関係数はCLAIM数で数える |
| 保留理由 | 辞書や構造化の改善に使う |

支持文書数と出現回数は分ける。同じ文書に3回現れても、支持文書数は1である。初期設定では、2文書以上で支持されたものを共通構造の候補として出力する。支持件数だけで原理の正しさを確定しない。

`A → B`と`A → B → C`は別パターンとして残す。両方が検出されるため、パターン数を独立した原理の数とは数えない。今後は包含関係も記録すると整理しやすくなるが、包含関係の明示的な管理は追加提案である。

## 5. 条件の違う構造の扱い（追加提案）

厳密な一致グループを作った後に、条件などが異なるものを関連候補として紐づける方法を追加できる。

```text
件数10以上なら A → B ── パターンP1
件数20以上なら A → B ── パターンP2
                              ↓
                  条件値だけが異なる関連候補
```

この場合も、P1とP2の支持文書数は別に保持する。条件を消して統合する処理や、閾値を一般化する処理は、現行の厳密一致とは分けて検証する。不確実性や文脈の違いも、まずは別グループとして残す。

この関連候補の紐づけは、現行実装で検証済みの機能ではない。

## 6. 実験で確認した範囲

今回の20件の実験では、上記の厳密一致で7種類の繰り返し構造が得られた。

| 比較条件 | 誤一致 | 取りこぼし |
|---|---:|---:|
| 意味・条件を保持する現行方式 | 0 | 0 |
| 概念IDを無視 | 8 | 0 |
| 否定を無視 | 1 | 0 |
| 条件を削除 | 14 | 0 |
| ノード型・接続role・向きだけ | 57 | 0 |
| 対象の種類まで一致必須 | 0 | 21 |

評価単位は、異なる文書にある断片同士の対応である。原文・構造化グラフ・期待対応を同じ作成者が設計した仮想データでの結果であり、実データでの精度を示すものではない。対象属性を常に無視してよいという根拠でもない。

原文からの自動構造化、意味や対象置換の妥当性、1,000件での処理性能、異分野への転用効果は今後の検証対象である。

## 関連ファイル

- [実験レポート](experiment20/実験レポート.md)
- [探索プログラム](experiment20/mine_patterns.py)
- [実行方法と制約](experiment20/README.md)
- [構造化後の処理フローと研究根拠](構造化後の処理フローと研究根拠_v0.1.md)
